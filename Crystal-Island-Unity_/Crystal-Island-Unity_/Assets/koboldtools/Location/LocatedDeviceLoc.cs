using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using KoboldTools;
namespace KoboldLocationTools
{/*
    public class LocatedDeviceLoc : ViewController<ILocated>
    {

        public override void onModelChanged()
        {
            Input.location.Start(1f);
        }

        private void Update()
        {
            if (Input.location.status != LocationServiceStatus.Running)
                return;

            model.latitude = Input.location.lastData.latitude;
            model.longitude = Input.location.lastData.longitude;
        }
    }*/
}
